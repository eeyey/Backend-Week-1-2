const capitalize = require('./capitalize');
const fixSpaces = require('./fixSpaces');
const getWordCount = require('./getWordCount');
const getUniWords = require('./getUniWords');

module.exports = { capitalize, fixSpaces, getWordCount, getUniWords };
